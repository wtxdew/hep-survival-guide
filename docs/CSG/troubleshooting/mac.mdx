---
id: troubleshooting-macos
title: Mac Issue
---

## Disable long press accent marks

- To enable the key repetition when holding a key-pressed, enter the following command in the command line.
    ```sh
    $ defaults write -g ApplePressAndHoldEnabled -bool FALSE
    ```
- Then logout and login to enable the setting.
